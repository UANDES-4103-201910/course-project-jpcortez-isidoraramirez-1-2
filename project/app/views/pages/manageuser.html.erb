<div class="container">
	<h1>Manage Users</h1>
	<% User.all.each do |user| %>

</br>
	<div class="card">
		  <div class="card-header">
		     <%= user.username %> (id:<%= user.id %>)
		  </div>
		  <div class="card-body">
		    <h5 class="card-title"><%= user.email %></h5>
		    <p class="card-text">Choose wisely.</p>
		        <div class="btn-group  pull-right">
		        	<% if user.admin == false %>
			       <a href="#" class="btn btn-primary btn-lg">Make Admin</a>
			        <% else %>
			    	<a href="#" class="btn btn-info btn-lg">Remove Admin</a>
			    	<% end %>
			        <a href="#" class="btn btn-danger btn-lg">Block User</a>
			        <a href="#" class="btn btn-dark  btn-lg">Blacklist</a>
    			</div>
		  </div>
	</div>
    <% end %>      	
</div>