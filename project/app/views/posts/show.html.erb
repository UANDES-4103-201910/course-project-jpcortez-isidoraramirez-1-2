
<div class="container" style="margin-left: 33%">
    <div class="row">
        <div class="[ col-xs-12 col-sm-offset-1 col-sm-5 ]">
			 <div class="[ panel panel-default ] panel-google-plus">
			<h2  style="margin-left: 33%">
			  <%= @post.title %>
			</h2>
			<h6 style="margin-left: 55%"><%= @post.user.email.split('@')[0].capitalize %></h6>

			<p style="margin-left: 33%" class="[ panel panel-default ] panel-google-plus">
			  <%= @post.body %>
			</p>

			<h2><%= @post.comments.count %> Comments </h2>
      <%= render @post.comments %><br>

			<%= link_to like_post_path(@post), class: "like-btn", method: :put, remote: :true do%>
			  <div><span><p><i class = "fa fa-thumbs-up fa-3x"></i></p></span></div>
			  <span class= "likes-count"> <%= @post.get_upvotes.size%>  </span>
			  <% end %>
			<% if current_user %>
			<% if @post.user.email == current_user.email || current_user.admin %>
			<%= link_to 'Edit', edit_post_path(@post) %>
			| <%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' } %>
			<h3>Add a comment:</h3>
			<%= render 'comments/form' %>
			<% end %>
			<% end %>
			<%= link_to 'Back', posts_path %>
			</div>
		</div>
	</div>
</div>
<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<p>
  <strong>User:</strong>
  <%= @post.user.email.split('@')[0].capitalize %>
</p>

<h2><%= @post.comments.count %> Comments</h2>
<%= render @post.comments %><br>
<%= link_to like_post_path(@post), class: "like-btn", method: :put, remote: :true do%>
  <div><span><p><i class = "fa fa-thumbs-up fa-3x"></i></p></span></div>
  <span class= "likes-count"> <%= @post.get_upvotes.size%>  </span>
  <% end %>
<% if current_user %>
<% if @post.user.email == current_user.email || current_user.admin || current_user.super %>
<%= link_to 'Edit', edit_post_path(@post) %>
| <%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' } %>
<h3>Add a comment:</h3>
<%= render 'comments/form' %>
<% end %>
<% end %>








<%= link_to 'Back', posts_path %>
>>>>>>> 698b3af00df1909de5ecf376a41c4d6386c1c94d
